<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title><%= message %></title>
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <style>
    pre { white-space: pre-wrap; }
  </style>
</head>
<body>
  <h1><%= message %></h1>
  <h2><%= (error && error.status) ? error.status : 500 %></h2>
  <% if (error && error.stack) { %>
    <pre><%= error.stack %></pre>
  <% } %>
  <% if (!error || !error.stack) { %>
    <p>No stack available.</p>
  <% } %>
  <p><a href="/">Back to home</a></p>
</body>
 </html>